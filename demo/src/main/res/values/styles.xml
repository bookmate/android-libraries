<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/Text</item>
        <item name="captionNoData">@string/no_data</item>
        <item name="loadingViewTextStyle">@style/LoadingViewText</item>
        <item name="emptyViewStyle">@style/LoadingViewText</item>
    </style>

    <style name="Text" parent="@android:style/TextAppearance">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="LoadingViewText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/holo_blue_bright</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="FloatingToolbar" />

    <style name="FloatingToolbar.SpecialButton" />

</resources>
